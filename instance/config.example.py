SECRET_KEY = "replace-with-a-long-random-secret"
ADMIN_PASSWORD = "replace-with-a-strong-admin-password"

# Optional: override the default SQLite location.
# DATABASE = "/srv/search-aggregator/instance/search_aggregator.sqlite3"

# Optional: trust one reverse proxy (for HTTPS scheme / host forwarding).
# TRUST_PROXY = True

# Optional: make generated absolute URLs prefer HTTPS.
# PREFERRED_URL_SCHEME = "https"
